spring:
#  security:
#    user:
#      name: user
#      password: devdojo
  config:
    import: optional:classpath:.env[.properties]
    profiles:
      active: mongo, mysql
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://localhost:3306/user_service?useTimezone=true&serverTimezone=UTC&createDatabaseIfNotExist=true
    username: ${ENV_ROOT_USER:root}  # Case ENV_ROOT_USER is not found apply root
    password: ${ENV_ROOT_PASSWORD:devdojo}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  threads:
    virtual:
      enabled: true

logging:
  level:
    academy.devdojo: debug
#    net.ttddyy.dsproxy.listener: debug database logs
#    org.hibernate.orm.jdbc.bind: trace
    org.springframework.security: debug
server:
  port: 8090

  servlet:
    context-path: /
  error:
    include-stacktrace: ON_PARAM #?trace=true
    include-binding-errors: always
    include-message: always
  tomcat:
    threads:
      max: 20

brasil-api:
  base-url: ${BRASIL_API_BASE_URL:https://brasilapi.com.br/api}
  cep-uri: ${BRASIL_API_URI:/cep/v1/{cep}}