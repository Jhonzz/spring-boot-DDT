<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>academy.devdojo</groupId>
        <artifactId>parent-project</artifactId>
        <version>0.0.1-SNAPSHOT</version>
        <relativePath/>
    </parent>

    <artifactId>anime-service</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>anime-service</name>
    <description>Projeto exemplo com Spring Boot</description>
    <properties>
        <anime-api-client.version>1.0-SNAPSHOT</anime-api-client.version>
        <jib-maven-plugin.version>3.5.2</jib-maven-plugin.version>
        <jib-maven-plugin.image>eclipse-temurin:21-jre</jib-maven-plugin.image>
        <docker.repo.url>jhonpablo</docker.repo.url>
        <docker.repo.project>${artifactId}</docker.repo.project>
        <docker.image.name>${docker.repo.url}/${docker.repo.project}</docker.image.name>
    </properties>


    <dependencies>
        <!--    commons-core   -->
        <dependency>
            <groupId>academy.devdojo</groupId>
            <artifactId>commons-core</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Spring Security -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- Spring Security tests -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>


        <!--  Open API config-->
        <dependency>
            <groupId>academy.devdojo</groupId>
            <artifactId>anime-api-client</artifactId>
            <version>${anime-api-client.version}</version>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>com.google.cloud.tools</groupId>
                <artifactId>jib-maven-plugin</artifactId>
                <version>${jib-maven-plugin.version}</version>
                <configuration>
                    <from>
                        <image>${jib-maven-plugin.image}</image>
                    </from>
                    <to>
                        <image>${docker.image.name}</image> <!-- jhonpablo/anime-service -->
                        <tags>
                            <tag>${project.version}</tag> <!-- version 0.0.1-SNAPSHOT (project's version-->
                        </tags>
                    </to>
                    <container>
                        <format>OCI</format> <!-- interoperability (used to run in different OS)  -->
                    </container>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>